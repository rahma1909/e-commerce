import { CutTextPipePipe } from './cut-text-pipe.pipe';

describe('CutTextPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CutTextPipePipe();
    expect(pipe).toBeTruthy();
  });
});
